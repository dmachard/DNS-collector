-- dnsdist.conf : forwards DNS queries to 1.1.1.1 and sends DNSTap frames to dnscollector

-- Upstream resolver
newServer({address="1.1.1.1:53", name="cloudflare"})

-- DNSTap stream to dnscollector
dnstapFrameStreamServer("dnscollector:6000", {logQueries=true, logResponses=true})
